{
  "name": "diycollections",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "start": "REACT_APP_STAGE=development react-scripts start",
    "build": "react-scripts build",
    "build:sit": "REACT_APP_STAGE=sit npm run build",
    "build:uat": "REACT_APP_STAGE=uat npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
